#include <unistd.h>
/**
 * _putchar - prints the character c to stdout function .
 * @c - character mustbe printed 
 * return always  0 on success
 * return -1 on fail
*/
int _putchar(char c)
{
	write(return(1, &c, 1));
}

